Uses: pam_sm_authenticate()

Only console/telnet login supported using a manual BitID login.

Fixme:
1. testing
2. Various example pam.d config files 
3. Announce on bitcointalk bitid thread and pam mailing list

Config file stuff:
- provide longer time to enter input
- allow retry on data entry error, re-prompt user for input on signature?

- option to use pam-bitid module for 'password' replacement of
  password prompt of normal login.
- setup login file so it falls back to default login.
- other pam apps, ssh, ruser, etc

* Set USER_PROMPT variable.
* some pam variable isn't being set right.
- when enabled pam modules: pam_securetty.so pam_nologin.so cause 
  username login prompt instead of bitcoin after failed input attempt.

Later:
- Add QR code generation
- enable a callback to allow using mobile device for signing
- option to check login against min bitcoin balance.

Testing:
- using various input string sizes, past max input.
- test by having bitid.access file missing
